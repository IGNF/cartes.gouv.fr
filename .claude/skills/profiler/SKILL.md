---
name: profiler
description: Lire les données du profileur Symfony pour un token donné (durées, collectors) directement dans le conteneur Docker
---

Lire `docs/developer/profiler.md` et appliquer la recette avec le token `$ARGUMENTS`.

Si aucun token n'est fourni, le demander (affiché dans l'en-tête `X-Debug-Token` ou la barre de debug Symfony).
